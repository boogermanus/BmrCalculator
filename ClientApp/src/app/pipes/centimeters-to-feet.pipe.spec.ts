import { CentimetersToFeetPipe } from './centimeters-to-feet.pipe';

describe('CentimetersToFeetPipe', () => {
  it('create an instance', () => {
    const pipe = new CentimetersToFeetPipe();
    expect(pipe).toBeTruthy();
  });
});
