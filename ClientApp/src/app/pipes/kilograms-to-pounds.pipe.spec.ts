import { KilogramsToPoundsPipe } from './kilograms-to-pounds.pipe';

describe('KilogramsToPoundsPipe', () => {
  it('create an instance', () => {
    const pipe = new KilogramsToPoundsPipe();
    expect(pipe).toBeTruthy();
  });
});
